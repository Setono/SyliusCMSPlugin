{% extends '@SyliusShop/layout.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block content %}
    <div class="sscms-page {{ page.identifier }}">{{ sscms_page(page.code) }}</div>
{% endblock %}
