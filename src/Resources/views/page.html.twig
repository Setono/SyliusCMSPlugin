{% extends '@SyliusShop/layout.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block content %}
    <div class="sscms-page {{ sscms_element_identifier(page) }}">{{ sscms_page(page.code) }}</div>
{% endblock %}
