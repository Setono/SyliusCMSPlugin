<div class="sscms-block sscms-block-non-existing sscms-block-{{ code }}" style="background-color: rgba(202,0,2,0.25)">
    The block <code>{{ code }}</code> does not exist. Please go to
    <a href="{{ path('setono_sylius_cms_admin_block_create', {'code': code}) }}">/admin</a> to create this block
</div>
