{{ form_errors(form) }}
<div class="ui two column stackable grid">
    <div class="column">
        <div class="ui segment">
            <div class="ui icon info message">
                <i class="info circle icon"></i>
                <div class="content">
                    <p>{{ 'setono_sylius_cms.form.block.necessary_help'|trans }}</p>
                </div>
            </div>

            {{ form_row(form.necessary) }}
            {{ form_row(form.service) }}
        </div>
    </div>
</div>
