sylius_grid:
    grids:
        setono_sylius_cms_admin_view:
            driver:
                options:
                    class: "%setono_sylius_cms.model.view.class%"
            limits: [100, 250, 500, 1000]
            fields:
                code:
                    type: string
                    label: sylius.ui.code
                template:
                    type: string
                    label: setono_sylius_cms.ui.template
            filters:
                search:
                    type: string
                    label: sylius.ui.search
                    options:
                        fields: [ code ]
            actions:
                main:
                    create:
                        type: create
                item:
                    update:
                        type: update
